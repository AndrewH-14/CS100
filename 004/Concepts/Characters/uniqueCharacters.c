#include <stdio.h>
#include <string.h>

int main() {

    char userInput[50];
    printf("Enter a string: ");
    fgets(userInput, 50, stdin);
    for(int i = 0; i < strlen(userInput); i++) {
        
    }

    return 0;
}