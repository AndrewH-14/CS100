#include <stdio.h>

int main() {

    int arr[100];
    int num;
    printf("How many integers?");
    scanf("%d\n", &num);
    printf("Enter %d unique integers (0 through 100):\n");

    for(int i = 0; i < num; i++) {
         scanf( "%d", arr[i]);
    }
    printf("The average is %lf\n", double average(int n, int x[], int &pMaxBelow))

    return 0;
}